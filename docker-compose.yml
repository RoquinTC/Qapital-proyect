# ============================================================
# Quid App — Docker Compose (Production)
# ============================================================
# Levanta únicamente la App de Quid.
# Aura-Standalone tiene su propio docker-compose en f:\Proyectos\Aura-Standalone\
# ============================================================

services:
  quid-app:
    build:
      context: ./Quid-App
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_VAPID_PUBLIC_KEY=${NEXT_PUBLIC_VAPID_PUBLIC_KEY}
    container_name: quid-app
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://quid:quid-local-dev@postgres:5432/quid?schema=public
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET:-change-me-in-production}
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:3000}
      - NEXTAUTH_TRUSTHOST=true
      - NEXT_PUBLIC_VAPID_PUBLIC_KEY=${NEXT_PUBLIC_VAPID_PUBLIC_KEY}
      - VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY}
      - VAPID_SUBJECT=${VAPID_SUBJECT:-mailto:quid@app.com}
      - AURA_API_KEY=${AURA_API_KEY:-AuraSecretKey2026}
      - OLLAMA_URL=${OLLAMA_URL:-http://host.docker.internal:11434/api}
      - AURA_MODEL=${AURA_MODEL:-llama3.2}
      - AUTH_COOKIE_SECURE=${AUTH_COOKIE_SECURE:-false}
      - CRON_SECRET=${CRON_SECRET}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - qapital-db:/data/db
    restart: unless-stopped

volumes:
  qapital-db:
